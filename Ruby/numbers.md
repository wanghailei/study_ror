# Numbers

Ruby integers are objects of class `Integer`. `Integer` objects hold integers that fit within the native machine word minus 1 bit. The internal storage of integers changes depending on the size of the number.